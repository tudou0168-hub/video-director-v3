diff --git a/src/video_director_v3/renderers/hyperframes/publish_templates.py b/src/video_director_v3/renderers/hyperframes/publish_templates.py
--- a/src/video_director_v3/renderers/hyperframes/publish_templates.py
+++ b/src/video_director_v3/renderers/hyperframes/publish_templates.py
@@
 def _wrap_strategy_scene(sid: str, role: str, scene: dict[str, Any], body: str) -> str:
@@
-    meta_html = _render_strategy_meta(layout_family, caption_mode, visual_object, memory_anchor, save_reason)
+    # P4.1B-R2 debug patch:
+    # Strategy meta is useful when inspecting scene_pack/render linkage, but it
+    # must not appear in normal publish previews. Keep it data-only by default
+    # and render it only when a scene explicitly opts into debug output.
+    meta_html = _render_strategy_meta(layout_family, caption_mode, visual_object, memory_anchor, save_reason) if _strategy_debug_enabled(scene) else ""
@@
     )
+
+
+def _strategy_debug_enabled(scene: dict[str, Any]) -> bool:
+    value = scene.get("debug_visual_strategy")
+    if isinstance(value, bool):
+        return value
+    if isinstance(value, str):
+        return value.strip().lower() in {"1", "true", "yes", "on", "debug"}
+    return False
@@
 def _render_strategy_meta(
@@
     return f"""
     <div class="vf-strategy-meta vf-strategy-meta--{layout_family}">
@@
diff --git a/src/video_director_v3/renderers/hyperframes/studio_native_project_builder.py b/src/video_director_v3/renderers/hyperframes/studio_native_project_builder.py
--- a/src/video_director_v3/renderers/hyperframes/studio_native_project_builder.py
+++ b/src/video_director_v3/renderers/hyperframes/studio_native_project_builder.py
@@
-        scene_html.append(f"""
-<section id="scene-{escape(sid.lower())}" class="scene clip role-{escape(role)} tpl-{escape(prev_template)} hf-bg-cinematic hf-bg-{escape(role)} {composition_class} vf-layout-{escape(layout_family)} vf-caption-{escape(caption_mode)} vf-ending-{escape(ending_variant or 'none')}" style="{escape(_scene_strategy_style(hud_scene))}" data-start="{start}" data-duration="{round(scene_duration, 3)}" data-track-index="1" data-visual-template="{escape(prev_template)}" data-role="{escape(role)}" data-layout-family="{escape(layout_family)}" data-caption-mode="{escape(caption_mode)}" data-ending-variant="{escape(ending_variant)}">
+        debug_visual_strategy = str(hud_scene.get("debug_visual_strategy") or "").strip().lower() in {"1", "true", "yes", "on", "debug"}
+        scene_html.append(f"""
+<section id="scene-{escape(sid.lower())}" class="scene clip role-{escape(role)} tpl-{escape(prev_template)} hf-bg-cinematic hf-bg-{escape(role)} {composition_class} vf-layout-{escape(layout_family)} vf-caption-{escape(caption_mode)} vf-ending-{escape(ending_variant or 'none')}" style="{escape(_scene_strategy_style(hud_scene))}" data-start="{start}" data-duration="{round(scene_duration, 3)}" data-track-index="1" data-visual-template="{escape(prev_template)}" data-role="{escape(role)}" data-layout-family="{escape(layout_family)}" data-caption-mode="{escape(caption_mode)}" data-ending-variant="{escape(ending_variant)}" data-debug-visual-strategy="{str(debug_visual_strategy).lower()}">
@@
-      <div class="hf-safe-zone" style="position:absolute;top:260px;left:72px;right:72px;">
+      <div class="hf-safe-zone vf-content-region vf-hook-region" style="position:absolute;top:var(--vf-safe-top,260px);left:72px;right:72px;">
@@
-      <div class="hf-safe-zone" style="position:absolute;top:220px;left:72px;right:72px;">
+      <div class="hf-safe-zone vf-content-region vf-problem-region" style="position:absolute;top:var(--vf-safe-top,220px);left:72px;right:72px;">
@@
-.caption.caption--minimal_caption{{font-size:38px;letter-spacing:-.02em;}}
-.caption.caption--emphasis_caption{{font-size:52px;border-width:2px;box-shadow:0 0 54px rgba(255,107,53,0.30), inset 0 1px 0 rgba(255,255,255,0.14);}}
-.caption.caption--quote_caption{{font-style:italic;letter-spacing:-.01em;}}
-.caption.caption--action_caption{{text-transform:none;letter-spacing:.01em;}}
+.caption.caption--minimal_caption{{
+  left:104px;right:104px;bottom:112px;
+  padding:14px 22px;
+  font-size:34px;line-height:1.22;letter-spacing:-.02em;font-weight:760;
+  background:rgba(2,4,12,0.50);border-color:rgba(56,225,255,0.28);
+  box-shadow:0 0 18px rgba(56,225,255,0.10), inset 0 1px 0 rgba(255,255,255,0.06);
+}}
+.caption.caption--emphasis_caption{{font-size:52px;border-width:2px;box-shadow:0 0 54px rgba(255,107,53,0.30), inset 0 1px 0 rgba(255,255,255,0.14);}}
+.caption.caption--quote_caption{{font-style:italic;letter-spacing:-.01em;background:rgba(2,4,12,0.62);}}
+.caption.caption--action_caption{{
+  text-transform:none;letter-spacing:.01em;
+  border-color:rgba(46,232,116,0.64);
+  box-shadow:0 0 46px rgba(46,232,116,0.20), inset 0 1px 0 rgba(255,255,255,0.12);
+}}
@@
-.vf-strategy-meta{{
+.vf-strategy-meta{{
+  display:none;
   position:absolute;top:var(--vf-meta-top,120px);right:var(--vf-meta-right,72px);left:var(--vf-meta-left,auto);width:var(--vf-meta-width,280px);
@@
 }}
+.scene[data-debug-visual-strategy="true"] .vf-strategy-meta{{display:block;}}
+
+/* P4.1B-R2 component reuse shim: layout family controls old mature HUD primitives
+   without rewriting the HyperFrames renderer. */
+.vf-layout-hero_metric .hf-big-title,
+.vf-layout-hero_statement .hf-big-title{{font-size:clamp(84px,10vw,124px);max-width:820px;}}
+.vf-layout-process_ladder .hf-safe-zone,
+.vf-layout-framework_map .hf-safe-zone{{top:var(--vf-safe-top,280px) !important;}}
+.vf-layout-tool_pipeline .hf-safe-zone,
+.vf-layout-config_panel .hf-safe-zone,
+.vf-layout-file_tree .hf-safe-zone{{top:var(--vf-safe-top,300px) !important;}}
+.vf-layout-comparison_board .hf-safe-zone,
+.vf-layout-proof_matrix .hf-safe-zone{{top:var(--vf-safe-top,300px) !important;}}
+.vf-layout-action_close .vf-ending-board,
+.vf-layout-insight_close .vf-ending-board,
+.vf-layout-checklist_close .vf-ending-board,
+.vf-layout-offer_close .vf-ending-board{{bottom:240px;}}
