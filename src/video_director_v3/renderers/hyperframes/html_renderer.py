"""HTML renderer - renders HyperFrames HTML from storyboard."""
from typing import Dict, Any


def render_html(
    storyboard: Dict[str, Any],
    design_profile: Dict[str, Any],
    motion_bindings: Dict[str, Any],
) -> str:
    """Render HTML from storyboard."""
    return "<html><body>Stub HTML</body></html>"