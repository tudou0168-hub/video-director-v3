"""Caption beat generator - generates caption timing beats."""
from typing import Dict, Any, List


def generate_caption_beats(
    narration_text: str,
    audio_duration: float,
    scene_count: int,
) -> List[Dict[str, Any]]:
    """Generate caption beats from narration."""
    return []